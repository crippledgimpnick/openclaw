# working-context

## Current focus
- Finalize the layered Obsidian-backed memory system for OpenClaw.
- Keep runtime-injected files compact and stable.
- Use the shared vault for larger operational context and cross-agent continuity.

## Current state
- Workspace is configured as an Obsidian vault.
- GitHub sync is configured at `git@github.com:crippledgimpnick/openclaw.git`.
- Dreaming is enabled and memory embeddings are working through Ollama.
- Layered memory scaffold exists in `Agent-Shared/`, `Agent-OpenClaw/`, and `OBSIDIAN_MEMORY_SYSTEM.md`.
- `BOOT.md`, `AGENTS.md`, `MEMORY_ARCHITECTURE.md`, and `OBSIDIAN.md` now point at the vault behavior.

## Next actions
- Explain the implementation clearly to Nick.
- If Nick wants a stricter Layer 1 budget, do a separate slimming pass on compact always-present files.

## Update rule
- Update this file at task start, after substantial progress, after compaction, and at task completion.
- Keep it brief, current, and action-oriented.
