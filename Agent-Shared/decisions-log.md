# decisions-log

Purpose: shared high-signal decisions that multiple agents may need later.

## 2026-04-19
- The workspace itself is the Obsidian vault.
- Git is the primary sync path for viewing the vault from another computer.
- Dreaming is enabled and uses Ollama embeddings (`nomic-embed-text`).
- `IDENTITY.md` owns role and orchestration doctrine.
- `AGENTS.md` owns workspace rules and file responsibilities.
- Orchestration is the default for substantial, multi-step, parallelizable, or long-running work, but simple direct tasks may be handled directly when delegation would add friction.

## Logging rule
- Add durable workspace or operating decisions here.
- Do not turn this into a timeline. Daily chronology belongs in daily logs.
