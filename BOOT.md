# BOOT.md

On startup:
- Use runtime-provided startup context first.
- If needed, orient with `IDENTITY.md`, `SOUL.md`, `USER.md`, and `MEMORY.md` in main sessions.
- Before project work, read the relevant project source-of-truth file.
- Follow `IDENTITY.md` for role and orchestration behavior.
- Follow `SOUL.md` for personality and judgment.
- If startup work sends a message, send it and then reply with `NO_REPLY`.
