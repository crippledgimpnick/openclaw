# BOOT.md

On startup:
- Use runtime-provided startup context first.
- If needed, orient with `IDENTITY.md`, `USER.md`, and `MEMORY.md` in main sessions.
- Before project work, read the relevant project source-of-truth file.
- Follow `SOUL.md` for personality and orchestration behavior.
- If startup work sends a message, send it and then reply with `NO_REPLY`.
